pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
    }
    plugins {
        kotlin("jvm") version ("1.7.22")
    }
}

rootProject.name = "advent-of-code-2022"
